[pytest]
# Only so the ported test file can stay byte-identical to the one in
# caura-ai/caura, which carries `pytestmark = [pytest.mark.unit]` from that
# repo's marker taxonomy. Registering the marker here is cheaper than editing
# the copy, and keeps drift between the two a one-line diff.
markers =
    unit: fast, no external services
